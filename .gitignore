# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/

# Editor / env
.vscode/
.env

# Rust build output
target/

# Agent iteration churn (not results)
**/_iterating.json
**/logs/

# rust-validator carries its own .git — versioned separately for now
tools/rust-validator/

# TWO-REPO SPLIT (2026-07-02): this repo tracks the product — agents,
# verifiers, twin, gate library, katas, docs. workspace/ (repo root) is the agents'
# local WORKSPACE: its own separate git repo (never pushed), where runs,
# flows, ADRs, logs, and CA ephemerals live. regress.py reads it from the
# filesystem, so the golden corpus lives there and regress stays a local test.
workspace/
docker-compose*.yml
compose.yaml

# Architect's private strategy notes — local only, never pushed
strategy.md
store.cypher
todo/